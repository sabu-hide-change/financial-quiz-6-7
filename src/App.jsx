// npm install lucide-react recharts firebase
import React, { useState, useEffect, useRef, useMemo } from "react";
import {
  Check,
  X,
  Home,
  ChevronRight,
  RefreshCw,
  BarChart2,
  BookOpen,
  User,
  ArrowRight,
  HelpCircle,
} from "lucide-react";
import {
  RadarChart,
  PolarGrid,
  PolarAngleAxis,
  PolarRadiusAxis,
  Radar,
  ResponsiveContainer,
  Tooltip,
} from "recharts";
import { initializeApp } from "firebase/app";
import { getAuth, signInAnonymously } from "firebase/auth";
import { getFirestore, doc, onSnapshot, setDoc } from "firebase/firestore";

/* ===================== 定数・設定 ===================== */
// 他の問題集アプリとデータが混ざらないよう一意の識別子を付与（後から一括書き換え可能）
const APP_ID = "QuizApp_Contract_And_Law_6_7_001";

// APIキー等は import.meta.env から取得（直書き厳禁）
const firebaseConfig = {
  apiKey: import.meta.env.VITE_FIREBASE_API_KEY,
  authDomain: import.meta.env.VITE_FIREBASE_AUTH_DOMAIN,
  projectId: import.meta.env.VITE_FIREBASE_PROJECT_ID,
  storageBucket: import.meta.env.VITE_FIREBASE_STORAGE_BUCKET,
  messagingSenderId: import.meta.env.VITE_FIREBASE_MESSAGING_SENDER_ID,
  appId: import.meta.env.VITE_FIREBASE_APP_ID,
};

// Firebase 初期化（未設定でもクラッシュさせず LocalStorage のみで動作させる）
let app = null;
let auth = null;
let db = null;
let firebaseReady = false;
try {
  if (firebaseConfig.apiKey) {
    app = initializeApp(firebaseConfig);
    auth = getAuth(app);
    db = getFirestore(app);
    firebaseReady = true;
    console.log("[init] Firebase initialized");
  } else {
    console.warn("[init] Firebase 未設定のため LocalStorage モードで動作します");
  }
} catch (e) {
  console.warn("[init] Firebase 初期化失敗。LocalStorage モードへフォールバック", e);
  firebaseReady = false;
}

/* ===================== 収録データ（完全ノンカット） =====================
 * 出典: スマート問題集 6-7 契約とその他の法律知識（中小企業診断士 経営法務）
 * 図表は外部画像を使わず、fig フィールドでインラインSVG／テーブルとして内製化。
 * note（ここが重要／解説レジュメ）は一切要約せず原文のまま格納する。
 * ==================================================================== */
const QUESTIONS = [
  {
    id: 1,
    source: "スマート問題集 6-7",
    kind: "4択問題",
    category: 1,
    title: "問題1 契約の成立",
    question: "契約に関する説明として、最も不適切なものはどれか。",
    choices: [
      { label: "ア", text: "契約とは、2つ以上の意思表示が合致することによって、当事者間に債権・債務（権利と義務）が発生するものである。" },
      { label: "イ", text: "原則として契約書などの書面がなくても契約は成立する。" },
      { label: "ウ", text: "未成年者は、行為能力が不十分（制限行為能力者）とされ、このような人が行った法律行為は取り消す事ができる場合がある。" },
      { label: "エ", text: "契約の内容の重要な部分について錯誤があった場合、その契約は錯誤した者が無効を主張することができる。" },
    ],
    answer: "エ",
    fig: null,
    note: `ここが重要
　本問では契約の成立について問われています。
　契約の基本になっている法律は、民法や商法ということになります。民法は条文が1,000以上あり、すべてを学ぶことは現実的ではありません。試験に必要な範囲に絞りこんで、効率よく学習することが大切です。
ア　○：　契約は、簡単に言えば当事者間で合意することです。法律的には、契約は2つ以上の意思表示が合致することによって、当事者間に債権・債務（権利と義務）が発生するものです。例えば、売買契約では、売り手の「売る」という意思表示と、買い手の「買う」という意思表示が合致、すなわち合意することで契約が成立します。また、商品の財産権を相手に引き渡す義務・権利と、これに対する代金の支払いという義務・権利が発生します。
イ　○：　原則として契約書などの書面がなくても契約は成立します。ただし、書面は契約の重要な証拠になるため、通常は書面によって契約を締結します。
ウ　○：　未成年者が単独で行った法律行為は、原則として取り消すことができます。法律行為を単独で行うことのできる能力のことを「行為能力」と呼びます。未成年者は、行為能力が不十分（制限行為能力者）とされ、このような人が行った法律行為は取り消す事ができます。法律行為を取り消した場合は、契約は無効になります。
エ　×：　意思表示の内容を勘違いしていた場合は、「錯誤」があると呼ばれます。契約の内容の重要な部分について錯誤があった場合、原則としてその契約は「取り消すこと」ができます。例えば、販売員が契約の重要な部分について説明せず、顧客が商品の品質を勘違いしていた場合は、契約の取り消しを主張できます。ただし、錯誤した者が注意を著しく欠いていた場合（重過失がある場合）は、契約の取り消しを主張できません。錯誤による意思表示は以前は「無効」とされていましたが、民法改正により「取り消すことができる」へと変更されました。無効と取消しの違いに注意してください。`,
  },
  {
    id: 2,
    source: "スマート問題集 6-7",
    kind: "4択問題",
    category: 1,
    title: "問題2 契約の分類",
    question: "契約に関する説明として、最も適切なものはどれか。",
    choices: [
      { label: "ア", text: "片務契約の一つとして雇用がある。" },
      { label: "イ", text: "双務契約の一つとして贈与がある。" },
      { label: "ウ", text: "書面によらない消費貸借は要物契約である。" },
      { label: "エ", text: "民法で定めている典型契約には贈与は入っていない。" },
    ],
    answer: "ウ",
    fig: null,
    note: `ここが重要
本問では契約の種類とその周辺について問われています。
民法は、私的自治の原則を採っています。私的自治の原則は、契約自由の原則とも呼ばれます。すなわち、契約などの行為を自分の自由な意思で行うことができることを表します。
他方、民法ではよく使用される契約を「典型契約」として規定しています。実務においては、典型契約以外の契約が無数にあります。民法はそれらの契約も典型契約と同様に認めています。いってみれば、典型契約は「ひな型」のようなものであると考えればよいでしょう。
ア　×：　片務契約は当事者一方のみに債務が生じる契約で、贈与、無償委任などがあります。雇用は双方に債務が生じるので双務契約になります。
イ　×：　双務契約は契約の当事者が互いに債務を負担する契約のことです。贈与は一方のみに債務が生じる片務契約になります。
ウ　○：　要物契約とは、契約の成立のために当事者の合意だけではなく物の引き渡しが必要となる契約です。消費賃借は借主が同じ種類、品質及び数量の物をもって返還をすることを約して、相手方から金銭その他の物を受け取ることを内容とする契約です。書面によらない消費貸借は要物契約です。書面による消費賃借は諾成契約になります。
エ　×：　典型契約には13種類あり、売買や賃貸借や贈与が入っています。贈与契約は「あげます」「もらいます」という意思表示が合致した契約です。`,
  },
  {
    id: 3,
    source: "スマート問題集 6-7",
    kind: "4択問題",
    category: 1,
    title: "問題3 契約の種類",
    question: "各種契約に関する説明として、最も不適切なものはどれか。",
    choices: [
      { label: "ア", text: "委任契約は、一方が相手方から依頼された仕事を完成させ、相手方がこれに対して報酬を与える契約である。" },
      { label: "イ", text: "使用貸借は、当事者の一方がある物を引き渡すことを約し、相手方がその物について契約が終了したときに返還をすることを約することによって、その効力を生ずる。" },
      { label: "ウ", text: "終身定期金契約は、一方が、自己、または相手方もしくは第三者の死亡に至るまで、定期的に金銭その他の物を相手方または第三者に給付する契約である。" },
      { label: "エ", text: "消費貸借契約を書面により締結した場合は、借主は、貸主から金銭を受け取るまでは契約を解除することができる。" },
    ],
    answer: "ア",
    fig: null,
    note: `ここが重要
　本問では各種の契約について問われています。
　一つ一つの契約の内容に踏み込む前に、どのような契約があるかを確認してください。そのなかで、共通の性質を持つ、似通った類型の契約はどこが異なるのか、整理しておく必要があります。
　たとえば、雇用契約と委任契約はともに労務を提供するという点で共通していますし、賃貸借契約と使用貸借契約はともに物を貸すという点で共通しています。
　それぞれどのように異なるのか整理しておいてください。賃貸借契約は、物を貸して賃料を得るわけですが、使用貸借契約では賃料を請求することはありません。
ア　×：　これは、委任契約ではなく、請負契約の説明です。委任契約は、仕事の完成が契約の内容にはなっていません。弁護士への訴訟の委任や、不動産売買の委任などが委任契約の例です。これに対し、請負契約は、建築工事やシステム開発など、業務を外部業者に外注する場合に良く用いられます。
イ　○：　使用貸借とは賃貸借のように有償で貸し借りすることではなく、無償で貸し借りすることです。使用貸借契約は当事者の一方がある物を引き渡すことを約し、相手方がその受け取った物について無償で使用及び収益をして契約が終了したときに返還をすることを約することによって、その効力を生じます。
ウ　○：　終身定期金契約は、一方が、自己、または相手方もしくは第三者の死亡に至るまで、定期的に金銭その他の物を相手方または第三者に給付する契約です。試験対策としては、出題されることの少ない契約類型ですが、この程度の知識は押さえておきましょう。
エ　○：　消費貸借契約は、民法改正により書面が締結された場合に成立する諾成契約となりました。そして借主は貸主からお金等の物を受け取るまでは、契約を解除することができます。`,
  },
  {
    id: 4,
    source: "スマート問題集 6-7",
    kind: "4択問題",
    category: 1,
    title: "問題4 契約の履行",
    question: "AはBから100万円のつぼを買い、まだその代金は支払っていない。この場合に関する説明として、最も不適切なものはどれか。",
    choices: [
      { label: "ア", text: "AはBに100万円を貸していた。この金銭債権の弁済期が到来していれば、Aは、Bの承諾なく、この金銭債権をもって、つぼの代金債務と相殺をすることができる。" },
      { label: "イ", text: "Aは100万円の宝石を所有していた。Aは、Bの承諾なく、この宝石をもって、つぼの代金債務の弁済にあてることができる。" },
      { label: "ウ", text: "Bは、このつぼの代金に関し、Aの承諾なく、Aの債務を免除することができる。" },
      { label: "エ", text: "Aは、一定の場合に、Bの承諾なく、供託所に100万円を預け、債務から免れることができる。" },
    ],
    answer: "イ",
    fig: null,
    note: `ここが重要
　本問では契約の履行について問われています。
契約成立後に、その内容が債務者によって履行されれば、債権・債務は消滅します。たとえば、100万円の金銭を借りている人が、期日に債権者にこれを返せばこれは「弁済」ということになり、債務は消滅します。しかし、これ以外にも、債権・債務が消滅する場合があります。代物弁済、供託、相殺、更改等です。それぞれの特徴を整理しておいてください。なお債権には、金銭債権の他に、特定物債権、種類債権、利息債権、選択債権があります。利息債権は利息の支払いを目的とする債権ですが、従来の民法の法定利率は年5％、商事法定利率は年6%でしたが、令和2年4月施行の改正民法にて、年3%に統合されて、さらに3年ごとに見直しして1%刻みで変動されるようになりました。
ア　○：　これは、相殺です。「相殺」は、債務者が債権者に対して同種の債権を有する場合に、その債務と債権を対当額で消滅させることです。相殺できるのは、同種の債権である必要があります。本肢は両方とも金銭債権ですので相殺できます。相殺は、一方的な意思表示で可能ですので、本肢のAはBの承諾なく相殺することができます。
イ　×：　Bの承諾があれば、代物弁済となります。「代物弁済」は、債務者が、債権者の承諾を得た上で、本来の給付に代えて他の給付をすることです。本肢は、Bの承諾を得ていないので成立しません。
ウ　○：　これは免除です。「免除」は、債権者が債権を無償で消滅させる意思表示をすることです。免除も債権の消滅原因の一つです。
エ　○：　これは供託です。「供託」は、債権者が受領を拒んだり、受領することができない場合に、債務者が供託所（法務局）に弁済目的物を預けることです。供託によって、債務が消滅します。`,
  },
  {
    id: 5,
    source: "スマート問題集 6-7",
    kind: "4択問題",
    category: 1,
    title: "問題5 保証",
    question: "AはBから100万円を借り受け、CはAの債務を連帯保証した。この場合に関する説明として、最も適切なものはどれか。",
    choices: [
      { label: "ア", text: "BC間の保証契約は、口頭でしたとしても、その効力を生じる。" },
      { label: "イ", text: "BがAに対する債権をDに譲渡し、その旨をAに通知したとき、DはAに対し100万円の請求はできるがCに対しては請求できない。" },
      { label: "ウ", text: "AがBに100万円の弁済をした場合、CのBに対する保証債務も消滅する。" },
      { label: "エ", text: "BがCに請求してきた際、CがAに弁済の資力があり、かつ執行が容易であることを証明すれば、BはまずAに履行を請求しなければならない。" },
    ],
    answer: "ウ",
    fig: "guarantee",
    note: `ここが重要
　本問では連帯保証について問われています。
　連帯保証は保証の一種であり、催告の抗弁権や検索の抗弁権がない、などの特徴がありますが、そのほかは一般の保証の性質を持っています。問題を解くときも、原則は一般の保証と同様に解いてください。
　このように3人以上の人が出てくる問題は、このように連帯保証は輪で結ぶなど図示すると考えやすくなります。たとえば本問では、このように書きます。
[[FIG]]
ア　×：　保証を行う場合は、債権者は、保証人と保証契約を書面または電磁的記録で締結する必要があります。すべての保証契約は、書面または電磁的記録でされない限り無効です。
イ　×：　これは保証債務の随伴性の問題です。随伴性は、主債務が債権譲渡等によって移転すれば、保証債務も一緒に移転することです。DはCに対し、100万円の請求ができます。
ウ　○：　これは保証債務の附従性の問題です。附従性は、主債務が消滅すれば保証債務も一緒に消滅することです。Aが弁済して主たる債務が消滅すれば、Cの保証債務も消滅します。
エ　×：　これは検索の抗弁権の問題です。連帯保証では、保証人には「催告の抗弁権」と「検索の抗弁権」がありません。したがって、Cはこのようなことは抗弁できません。`,
  },
  {
    id: 6,
    source: "スマート問題集 6-7",
    kind: "4択問題",
    category: 1,
    title: "問題6 契約の不履行",
    question: "AはBに1000万円の債務を負っている。この場合に関する説明として、最も不適切なものはどれか。",
    choices: [
      { label: "ア", text: "Aは事業資金としてBから1000万円借り受け、10月1日に返済する予定であったが、失念している。これは履行遅滞の問題である。" },
      { label: "イ", text: "Aは事業資金としてBから1000万円借り受け、10月1日に返済する予定であったが、事業に失敗し、全ての財産を清算したが、1000万円は絶対に返済できないことが分かった。これは履行不能の問題である。" },
      { label: "ウ", text: "AはBから別荘を買ったが、引渡しを受ける前日に山火事で別荘が焼失した。これは、危険負担の問題である。" },
      { label: "エ", text: "AはBから別荘を買い、引渡しを受けた後に別荘にBも知らない白アリが巣食っていることを発見した。これは、契約不適合責任の問題である。" },
    ],
    answer: "イ",
    fig: null,
    note: `ここが重要
　本問では契約の不履行について問われています。
　契約が履行されない場合、さまざまな問題が考えられます。どの領域の問題であるのかをしっかり整理する必要があります。
　本問を通じて、契約不履行のケースを整理してください。
ア　○：　これは、「債務不履行」のうち、「履行遅滞」にあたります。履行遅滞は、契約で定めた履行期に遅れることです。履行遅滞は、期日に遅れたものの履行自体は可能な状況の場合を表します。
イ　×：　これは｢履行不能」の問題ではなく、「履行遅滞」にあたります。履行不能は、契約で定めた履行が不能になることです。例えば、売買目的の物を、債務者の不注意で紛失してしまい、引き渡しが不能になるような場合です。金銭債権の場合、お金自体がなくなったわけではないので、履行不能の問題は起きません。金銭債権では履行不能の問題は起きないので、選択肢イの記述は不適切です。
ウ　○：　これは「危険負担」の問題です。売買契約締結後、引渡しの前までに、売主が責を負わない事由(台風で建物が倒壊した、あるいは隣家の失火によって建物が類焼した等)によって売主の引渡義務が履行できなくなった場合に、買主の代金支払債務が消滅するのか、しないのかの問題です。危険負担は、債務者主義が原則です（民法改正により特定物の債権者主義は削除）。本肢の場合、危険は引渡債務者である売主Ｂの負担となり、Ａは契約を解除して代金支払を拒むことができます。
エ　○：　これは「契約不適合責任」の問題です（民法改正前の瑕疵担保責任）。契約不適合責任は、売買契約等で、契約は履行されたものの、目的物に欠陥などの契約不適合があった場合の、売主の責任を指します。売主の故意・過失を要件とせず、たとえ売主が契約不適合を知らなかった場合でも、買主は、目的物の修補、代替物または不足分の引渡しによる履行の追完や代金減額を請求することができる点に注意してください。`,
  },
  {
    id: 7,
    source: "スマート問題集 6-7",
    kind: "4択問題",
    category: 1,
    title: "問題7 契約の不履行2",
    question: "AはBに絵画を売却した代金として100万円の債権を持っているが、期日になってもBは支払わない。この場合に関する説明として、最も不適切なものはどれか。",
    choices: [
      { label: "ア", text: "Aは強制履行により代金を取り立てることができる。" },
      { label: "イ", text: "AはBに対して損害賠償の請求ができる。損害賠償請求権の消滅時効は、原則として権利を行使できることを知った時から５年間である。" },
      { label: "ウ", text: "AはBとの契約を解除することができる。その場合は、損害賠償の請求はできなくなる。" },
      { label: "エ", text: "契約が解除された場合は、契約は最初から無かったものとされるため、A・Bは互いに受け取っていた物や金銭を返却する必要がある。" },
    ],
    answer: "ウ",
    fig: null,
    note: `ここが重要
　本問では債務不履行の対抗手段について問われています。
　債務不履行がなされた場合、債権者としては、強制履行、損害賠償請求、契約の解除、等の手段をとることが認められています。
　債務不履行と対抗手段の関係について、整理しておきましょう。
ア　○：　債務不履行が生じた場合、債権者は、強制履行や損害賠償請求、契約の解除といった手段を取ることができます。強制履行は、債権者が、国家機関の力を借りて強制的に履行をさせることです。強制履行には、「直接強制」、「代替執行」、「間接強制」という3つの方法があります。
イ　○：　債務不履行が生じた場合には、債権者は債務者に対して損害賠償を請求することができます。ただし、金銭債務以外の債務不履行につき損害賠償請求ができるのは、債務者に故意または過失がある場合に限ります。債務不履行による損害賠償請求権の消滅時効は、原則として権利を行使できることを知った時から５年、権利を行使することができる時から10年とされています。
ウ　×：　債務不履行の場合は、債権者は債務者の承諾を要さずに、一方的に契約を解除することができます。このように、合意によらず契約を解除できることを「法定解除権」と呼びます。しかし、解除権を行使したのちも損害があればその賠償請求はできますので、その部分が誤りです。
エ　○：　契約が解除された場合は、契約は最初から無かったものとされるため、当事者同士は互いに受け取っていた物や金銭を返却する必要があります。これを原状回復義務と呼びます。`,
  },
  {
    id: 8,
    source: "スマート問題集 6-7",
    kind: "4択問題",
    category: 1,
    title: "問題8 不法行為",
    question: "Aは歩行中、わき見運転をしていたピザ宅配会社B社の社員Cのバイクにはねられ、負傷した。この場合に関する説明として、最も不適切なものはどれか。",
    choices: [
      { label: "ア", text: "Aは、治療費等の損害をCに対し、賠償請求することができる。" },
      { label: "イ", text: "Aは、Cがピザの配達中の事故であれば、Bに対して損害賠償の請求ができる。" },
      { label: "ウ", text: "Bは、Cの選任・監督について相当な注意をしたときは、Aに対する損害賠償責任を負わない。" },
      { label: "エ", text: "Aは、損害及び加害者を知らない限り、損害賠償請求権は消滅することはない。" },
    ],
    answer: "エ",
    fig: null,
    note: `ここが重要
　本問では不法行為に関連して、使用者責任や損害賠償請求権の消滅時効について問われています。
不法行為は、故意または過失によって他人の権利を侵害し、損害を与えることです。例として、不注意で交通事故を起こして、他人の所有物を壊してしまうことなどが挙げられます。
不法行為が成立した場合、加害者は被害者の被った損害を賠償する義務を負います。
不法行為に関連して使用者責任等の知識に関する出題されていますので、しっかり押さえておく必要があります。
ア　○：　一般不法行為の問題です。不法行為が成立するにはいくつかの要件があります。それは、被害者に損害が発生していること、加害者に故意または過失があること、加害者に責任能力があること、加害行為が違法なものであること、損害と加害行為の間に因果関係があることです。本設問では、これらを否定するようなことがらは見受けられないので、本肢は正しいです。
イ　○：　使用者責任の問題です。会社の従業員が、その会社の事業を執行する上で不法行為を行った場合、従業員の使用者である会社は、損害賠償責任を負います。これを使用者責任と呼びます。本肢において、ピザの配達中の事故であれば、Bの業務の執行中であるので、AはBにも損害賠償請求ができます。
ウ　○：　民法は、使用者責任において、使用者が従業員の選任・監督について相当な注意をした時は、使用者は責任を負わなくても良いと規定しています。
エ　×：　人の生命または身体に対する不法行為による損害賠償請求権の消滅時効は、被害者または法定代理人が、損害および加害者を知ったときから５年となっています。さらに、不法行為の時から20年を経過した場合も、権利は消滅します。したがって、いつまでも損害賠償の請求ができるわけではありません。`,
  },
  {
    id: 9,
    source: "スマート問題集 6-7",
    kind: "4択問題",
    category: 1,
    title: "問題9 担保物権",
    question: "担保物権に関する説明として、最も不適切なものはどれか。",
    choices: [
      { label: "ア", text: "抵当権には、優先弁済効力はあるが、留置的効力はない。" },
      { label: "イ", text: "質権には、優先弁済効力はあるが、留置的効力はない。" },
      { label: "ウ", text: "留置権には、優先弁済効力はないが、留置的効力はある。" },
      { label: "エ", text: "先取特権には、優先弁済効力はあるが、留置的効力はない。" },
    ],
    answer: "イ",
    fig: "securityTable",
    note: `ここが重要
　本問では担保物権について問われています。
制限物権には用益物権と担保物権の2つの類型があります。担保物権は名前のとおり保証的意味合いをもつ物権で、民法では以下の4つが定められています。
●留置権
他人の物を占有する者が、その物に関する債権の弁済を受けるまで、その物を留置することができる権利。
●先取特権
一定の債権を有する者が、他の債権者に先立って自己の債権の弁済を受けることができる権利。
●質権
債権の担保として受け取った物を占有し、その物について優先的に弁済を受けることができる権利。
●抵当権
債務の担保に供した物について、優先的に弁済を受ける権利。質権と違い、引渡しを要しない。
担保物権にはさまざまな効力がありますが、なかでも優先弁済効力と留置的効力が重要です。
●優先弁済効力
他の一般債権者に優先して弁済を受けられる効力。
●留置的効力
担保物権の対象となる物の占有を保持することができる効力。
これらを整理すると次の表のようになります。
[[FIG]]
担保物権について基本的なところは押さえておくようにしましょう。
ア　○：　上表の通りです。抵当権には、優先弁済効力はありますが、留置的効力はありません。
イ　×：　上表の通りです。質権には、優先弁済効力と留置的効力の両方があります。
ウ　○：　上表の通りです。留置権には、留置的効力はありますが、優先弁済効力はありません。
エ　○：　上表の通りです。先取特権には、優先弁済効力はありますが、留置的効力はありません。`,
  },
  {
    id: 10,
    source: "スマート問題集 6-7",
    kind: "4択問題",
    category: 2,
    title: "問題10 独占禁止法",
    question: "独占禁止法に関する説明として、最も不適切なものはどれか。",
    choices: [
      { label: "ア", text: "独占禁止法は、一部の企業による独占や、不公正な取引などを規制する法律で、公正で自由な競争を促進することで、一般消費者の利益を実現することを目的としている。" },
      { label: "イ", text: "私的独占は、事業者が強力な資金力を用いて、他の事業者を市場から排除したり、支配することである。" },
      { label: "ウ", text: "不公正な取引方法というのは、公正な競争を阻害するおそれがあるもので、法律に規定される取引方法や公正取引委員会が指定した取引方法がある。その内容はたとえば、取引拒絶、不当廉売、抱き合わせ販売などであり、高額で購入することは含まれない。" },
      { label: "エ", text: "カルテルや入札談合等に関与した事業者に対して、違法行為の申し出や、調査協力をした場合に、課徴金が減免される制度がある。" },
    ],
    answer: "ウ",
    fig: null,
    note: `ここが重要
　本問では独占禁止法について問われています。
　独占禁止法では、私的独占、不当な取引制限、不公正な取引方法を規制しています。
　それぞれの類型ごとにどのような規制がなされているのか、しっかり整理しておく必要があります。
ア　○：　独占禁止法は、一部の企業による独占や、不公正な取引などを規制する法律です。公正で自由な競争を促進することで、一般消費者の利益を実現することを目的としています。独占禁止法では、私的独占、不当な取引制限、不公正な取引方法を規制しています。
イ　○：　私的独占は、事業者が強力な資金力を用いて、他の事業者を市場から排除したり、支配することです。たとえば、優越した市場支配力を得た事業者がその力を利用してダンピングや差別価格を設定して、他の事業者を市場から締め出したり、自由な判断で活動することを妨げることを指します。
ウ　×：　不公正な取引方法というのは、公正な競争を阻害するおそれがあるもので、法律に規定される取引方法や公正取引委員会が指定した取引方法です。例えば、取引拒絶、不当廉売、抱き合わせ販売などの行為があります。不当高価購入も不公正な取引方法の一つであり、高額での購入がこれに当たる可能性があります。
エ　○：　私的独占、カルテル及び一定の不公正な取引方法については、課徴金が課されます。課徴金とは違反した企業に課せられる罰金制度です。しかし、公正取引委員会に、事業者自らが関与した違法行為の申出、調査協力をした場合、その企業の課徴金が減免される「課徴金減免制度」が適用されることがあります。`,
  },
  {
    id: 11,
    source: "スマート問題集 6-7",
    kind: "4択問題",
    category: 2,
    title: "問題11 消費者保護に関する法律",
    question: "消費者保護法制に関する説明として、最も不適切なものはどれか。",
    choices: [
      { label: "ア", text: "消費者契約法は、事業者と消費者の契約の際に、契約の重要事項について不実告知や、不当な勧誘がされたことにより、消費者が誤認または困惑して契約をした場合には、その契約は無効である旨を規定している。" },
      { label: "イ", text: "消費者契約法は、事業者は消費者にとって一方的に不利益な契約条項を定めることはできないと規定する。" },
      { label: "ウ", text: "割賦販売法は、割賦販売では、消費者が後で申込みの撤回ができる「クーリング・オフ」の制度を設けている。" },
      { label: "エ", text: "特定商取引法は、訪問販売や通信販売、電話勧誘販売、連鎖販売といった特定商取引について、それぞれに規制を設ける法律である。" },
    ],
    answer: "ア",
    fig: null,
    note: `ここが重要
　本問では消費者保護に関する法律について問われています。
　民法では、原則として、当事者の特性は無視して、平等に扱われます。性別や財力の違いなど一切問わないことが建前として認められています。しかし、現実的には、強力な資金力や商品知識を充分に有する企業と消費者の間には大きな差があります。そのために、商品知識が無い消費者が、企業にだまされることが起こりえます。
　その対策として、消費者を保護するための各種の法律が制定されています。ここでは、それらの法律のうち、試験で重要なポイントを学習します。
ア　×：　消費者契約法は、事業者と消費者の契約に適用される法律です。事業者と消費者の契約の際に、契約の重要事項について不実告知や、不当な勧誘がされたことにより、消費者が誤認または困惑して契約をした場合には、消費者は契約を取り消すことができます。無効になるわけではありませんので、選択肢アの記述は不適切です。
イ　○：　消費者契約法は、事業者は消費者にとって一方的に不利益な契約条項を定めることはできないと規定しています。たとえば、事業者側の債務不履行によって消費者側に生じた損害を賠償する責任を免除する特約などがこれにあたります。
ウ　○：　割賦販売法は、分割払いによる商品の販売の際に適用される法律です。割賦販売では、業者は、割賦販売の条件などの情報開示や、契約書などの書面の交付が必要になります。また、消費者が後で申込みの撤回ができる「クーリング・オフ」の制度を設けています。
エ　○：　特定商取引法は、訪問販売や通信販売、電話勧誘販売、連鎖販売といった特定商取引について、それぞれに規制を設ける法律です。特定商取引法でも、書面の交付義務や、クーリング・オフの制度などを設けています。`,
  },
  {
    id: 12,
    source: "スマート問題集 6-7",
    kind: "4択問題",
    category: 2,
    title: "問題12 製造物責任法",
    question: "製造物責任法に関する説明として、最も不適切なものはどれか。",
    choices: [
      { label: "ア", text: "製造物責任法では、製造業者の過失を要件とせず、製造物に欠陥があったことのみで、損害賠償責任を追及できる。" },
      { label: "イ", text: "製造物にその製造業者と誤認させるような氏名の表示をしただけの者は、製造物責任法上の責任を負わない。" },
      { label: "ウ", text: "製造物責任法に基づく損害賠償請求権は、被害者またはその法定代理人が損害および賠償義務者を知った時から３年間を行使しないときは、時効によって消滅する。" },
      { label: "エ", text: "製造物責任法における製造物には、未加工の農林畜水産物は該当しない。" },
    ],
    answer: "イ",
    fig: null,
    note: `ここが重要
　本問では製造物責任法について問われています。
製造物責任法とは、製造業者に対して製造物の欠陥があった際の無過失責任を負わせて、損害賠償請求を消費者ができることを定めた法律です。
そこでは、製造業者の責任を無過失責任であることや、損害賠償を負う製造物の範囲、製造業者の定義などが定められています。テキストで確認しておいてください。
ア　〇：　製造物責任法では、製造業者の責任を無過失責任とし、製造物に欠陥があったことのみで、損害賠償責任を追及できるようにしています。よって選択肢アの記述は適切です。
イ　×：　製造物責任法では、実際に製造物を製造、加工、輸入していなくても、製造物にその氏名、商標等の表示をした者や、その製造業者と誤認させるような氏名の表示をした者を、製造業者等として製造物責任を負うこととしています。よって選択肢イの記述は不適切です。
ウ　〇：　製造物責任法に基づく損害賠償請求権は、被害者またはその法定代理人が損害および賠償義務者を知った時から３年間を行使しないとき（人の生命または身体の侵害への損害賠償の場合は５年）、またはその製造業者等が当該製造物を引き渡した時から10年を経過した時は、時効によって消滅してしまう。よって選択肢ウの記述は適切です。
エ　〇：　製造物責任法における製造物は、「製造又は加工された動産」と定義され、サービス、不動産、コンピュータプログラム、未加工農林畜水産物などは含まれません。よって選択肢エの記述は適切です。
以上より、選択肢イが正解となります。`,
  },
  {
    id: 13,
    source: "スマート問題集 6-7",
    kind: "4択問題",
    category: 2,
    title: "問題13 景品表示法",
    question: "景品表示法に関する説明として、最も不適切なものはどれか。",
    choices: [
      { label: "ア", text: "優良誤認表示とは、商品・サービスの内容が、実際のものや競合他社よりも著しく優良であると誤認させることであり、有利誤認表示とは、 商品・サービスの価格や取引条件が、実際のものや競合他社よりも著しく有利であると誤認させることである。" },
      { label: "イ", text: "内閣総理大臣は、景品表示法の違反行為があるとみとめたときは、その事業者に対して、誤認の排除，再発防止策の実施，今後同様の違反行為を行わないことなどを命ずる措置命令をすることができる。" },
      { label: "ウ", text: "商店街が実施する福引などの共同懸賞は、以前からの風習であることや、個別事業者の不当な利益の獲得にはつながりにくいことなどから、景品規制から除外されている。" },
      { label: "エ", text: "景品表示法における景品類は、顧客を誘引する手段として、事業者が自己の供給する商品・サービスの取引に付随して提供する、物品・金銭その他の経済上の利益を指す。" },
    ],
    answer: "ウ",
    fig: null,
    note: `ここが重要
　本問では景品表示法について問われています。
　景品表示法は、正式には、不当景品類及び不当表示防止法といいます。景品表示法では、大きく分けて、過大な景品類の提供の禁止・不当な表示の禁止の二つの行為の禁止が謳われています。
　消費者が過剰な景品に惑わされて対価に見合わない商品やサービスを購入することがないよう、景品類の最高額や総額などを規定している法律です。
ア　○：　適切な記述です。景品表示法における「不当な表示の禁止」は、「優良誤認表示」の禁止、「有利誤認表示」の禁止、「その他誤認されるおそれのある表示」の禁止の3つに分けられます。優良誤認表示は、商品やサービスの品質や内容が事実に反して優良であると表示するものです。有利誤認表示は、商品やサービスの価格や取引条件が事実に反して有利であると表示するものです。
イ　○：　適切な記述です。不当な表示の禁止・過大な景品類の提供の禁止のいずれかの規制に抵触した事業者に対して、内閣総理大臣（または内閣総理大臣から委任を受けた消費者庁長官）は「措置命令」をすることができます。なお、不当な表示の禁止の規制に抵触した事業者に対しては、措置命令に加えて、課徴金納付命令を下すこともあります。
ウ　×：　商店街が実施する福引などの共同懸賞も、景品類のひとつとして景品表示法の規制対象になっています。景品表示法においては、景品類を一般懸賞、共同懸賞、総付景品の3つに分けて、景品の最高額や総額を規制しています。懸賞とは、くじや抽選、じゃんけん、ゲームなどの結果に応じて景品類を提供することを指します。複数の事業者が参加して行う場合を共同懸賞と呼び、それ以外の場合を一般懸賞と呼びます。総付景品とは、懸賞によらず全ての利用者や購入者、来店者などにもれなく提供する景品類のことを指し、先着順や申し込み順で提供されるものもこれにあたります。
エ　○：　適切な記述です。景品表示法における景品類とは、「顧客を誘引するための手段であること」「取引に附随して提供されるものであること」「経済上の利益であること」を満たす場合に該当します。ただし、通常の商慣習の範囲での値引きやアフターサービスなどは景品には含まれません。`,
  },
  {
    id: 14,
    source: "スマート問題集 6-7",
    kind: "空欄補充",
    category: 2,
    title: "問題14 国際取引",
    question: `国際取引に関する次の文中の空欄Ａ～Ｄに入る語句の組み合わせとして、最も適切なものを下記の解答群から選べ。
（　Ａ　）では、国際貿易の統一規則が規定されている。（　Ｂ　）とは、商品を本船に船積された時に、引渡し義務が終了する取引条件である。CIFでは、（　Ｃ　）は、貨物を荷揚げ地の港で荷揚げするまでの運賃、海上保険料等を負担し、荷揚げ以降の費用は（　Ｄ　）の負担となる。`,
    choices: [
      { label: "ア", text: "Ａ：インコタームズ　Ｂ：FOB　Ｃ：買主　Ｄ：売主" },
      { label: "イ", text: "Ａ：インコタームズ　Ｂ：FOB　Ｃ：売主　Ｄ：買主" },
      { label: "ウ", text: "Ａ：準拠法　Ｂ：CFR　Ｃ：買主　Ｄ：売主" },
      { label: "エ", text: "Ａ：準拠法　Ｂ：CFR　Ｃ：売主　Ｄ：買主" },
    ],
    answer: "イ",
    fig: "fobcif",
    note: `ここが重要
　本問では国際取引について問われています。
　国際取引の知識は重要です。本問を通して重要な用語を整理しておきましょう。
　●FOB（Free on board)
　貿易における取引条件のひとつです。FOBとは、Free on Boardの略で、本船渡しのことです。船舶の手配、保険料等は買主の義務になります。
　●CIF（Cost Insurance and Freight）
　貿易における取引条件のひとつです。CIFとは、Cost Insurance and Freightの略で、運賃保険料込みのことです。運賃、保険料を売主が負担する条件です。
　●インコタームズ（International Commercial Terms)
　インコタームズ (Incoterms) では、国際商業会議所 (ICC) が策定した国際貿易の統一規則が規定されています。貿易取引を円滑に行うため、運賃、保険料等の条件に関して、国際的に統一的な定義を取り決めています。上記のFOBやCIFもインコタームズで定められています。
　●準拠法
　外国企業との取引で、紛争が発生した時に、どこの国の法律が適用されるのかを決めなければなりません。この適用になる国の法律を準拠法といいます。
　国際取引に関する領域は非常に幅が広いので注意してください。対策としては、本問で取り上げた用語など、重要な項目に絞って押さえておきましょう。
Ａ～Ｄに入る語句は次のようになります。
Ａ：インコタームズ
　国際貿易の統一規則はインコタームズです。なお、策定機関である国際商業会議所 (ICC) も押さえておきましょう。
Ｂ：FOB
　商品を本船に船積された時に、引渡し義務が終了する取引条件はFOBです。
Ｃ：売主　Ｄ：買主
　CIFでは、売主は貨物を荷揚げ地の港で荷揚げするまでの費用を負担し、荷揚げ以降の費用は買主の負担となります。CIFとFOBの違いを整理して押えておきましょう。
[[FIG]]
以上より、正解はイとなります。`,
  },
  {
    id: 15,
    source: "スマート問題集 6-7",
    kind: "4択問題",
    category: 2,
    title: "問題15 国際条約、国際出願",
    question: "知的財産権に関する国際条約や国際出願に関する記述として、最も適切なものはどれか。",
    choices: [
      { label: "ア", text: "国際出願では、各国で審査を受けるに際し、所定の翻訳文を提出する等の国内移行手続をまず行う必要がある。" },
      { label: "イ", text: "マドリッド協定議定書は、商標の国際出願制度であり、日本での商標登録出願をしなくても制度を利用して外国での商標権の取得をすることができる。" },
      { label: "ウ", text: "マドリッド協定議定書に基づいて国際出願を行う場合の出願する商標と基礎となる商標は、同一性のある範囲なら変更することができる。" },
      { label: "エ", text: "パリ条約に規定する優先権の期間について、商標に認められる期間は12か月である。" },
    ],
    answer: "ア",
    fig: null,
    note: `ここが重要
　本問では知的財産に関する国際条約や国際出願について問われています。
国際条約・国際出願に関しては幅広くさまざまな知識が問われているため、過去に出題されたことのある内容について本問を通じて理解を深めておきましょう。
最近では、パリ条約、マドリッド協定、PCTが出題されていますので、その内容についてしっかりと押さえておきましょう。
ア　○：　国際出願では、各国で審査を受けるに際し、所定の翻訳文を提出する等の国内移行手続をまず行う必要があります。
イ　×：　マドリッド協定議定書は、日本が加盟している商標の国際出願制度です。外国での商標権の取得が日本の特許庁での手続で可能ですが、特許庁に出願もしくは登録されている国内の商標を基礎とします。そのため、日本での商標登録出願が必要です。なお、日本での出願と並行して国際登録出願をすることもできます。
ウ　×：　マドリッド協定議定書に基づいて国際出願を行う場合の出願する商標と基礎となる商標は、同一でなければなりません。
エ　×：　パリ条約は、特許権、実用新案権、意匠権、商標権といった産業財産権の国際的な保護を目的とした条約です。ある加盟国にて出願をした者が、別の加盟国に同一の出願をした場合、最初の国での出願の時を基準として、新規性や進歩性のある発明かどうかなどの判断が行われます。いわゆる優先権制度を定めています。ただし、この優先権は最初の出願の日から一定期間内に申請を行った場合に適用されるものです。特許・実用新案は12か月以内、意匠、商標は6か月以内であれば、その出願は最初の出願をした日に出願したものとして取り扱われます`,
  },
  {
    id: 16,
    source: "スマート問題集 6-7",
    kind: "4択問題",
    category: 2,
    title: "問題16 倒産に関する法律1",
    question: "倒産に関する法律について、最も不適切なものはどれか。",
    choices: [
      { label: "ア", text: "倒産は企業が経済的に破たんすることであり、一般的には、手形が不渡になって銀行取引停止処分となった場合などをさす。" },
      { label: "イ", text: "特別清算は、清算中の株式会社について、清算の遂行に著しい支障があるか、債務超過の疑いがある時に行われる特殊な清算手続きである。" },
      { label: "ウ", text: "民事再生は、経済的に窮地にある債務者の事業の再生をすることを目的とし、法人だけでなく個人も行うことができる。" },
      { label: "エ", text: "会社更生は、倒産に至る前の比較的早期の段階にある会社を再建する手続きで、全ての会社が対象で、個人は行うことができない。" },
    ],
    answer: "エ",
    fig: null,
    note: `ここが重要
　本問では倒産に関する法律について問われています。
　倒産はその企業にとって最大の問題であるとともに、企業の債権者にとっても債権の回収ができなくなるなど、困難な状況となります。法律は、倒産について、破産法、民事再生法、会社更生法などで規制をしています。
　それぞれの法律について、対象や処理の仕方を、横断的に整理することがポイントです。
ア　○：　倒産は企業が経済的に破たんすることです。ただし、法律的には「倒産」という概念は厳密に規定されていません。一般的には、手形が不渡になって銀行取引停止処分となった場合などに倒産という言葉が使われています。倒産となると、債権者は債権の弁済を受けられなくなるなどの大きな影響があります。よって、何らかの形で倒産処理を行う必要があります。
イ　○：　清算型の倒産処理には、破産と特別清算があります。特別清算は、清算中の株式会社について、清算の遂行に著しい支障があるか、債務超過の疑いがある時に行われる特殊な清算手続きです。
ウ　○：　再建型の倒産処理には、民事再生と会社更生があります。民事再生は、経済的に窮地にある債務者の事業の再生をすることを目的としています。民事再生は、法人だけでなく個人も行うことができます。
エ　×：　会社更生は、倒産に至る前の、比較的早期の段階にある株式会社を再建する手続きです。会社更生は、大規模な企業が多い株式会社では、倒産すると社会的な影響が大きいことから、株式会社に限って認められている倒産処理です。すべての会社に適用があるわけではないので、誤りです。`,
  },
  {
    id: 17,
    source: "スマート問題集 6-7",
    kind: "空欄補充",
    category: 2,
    title: "問題17 倒産に関する法律2",
    question: `倒産に関する次の文中の空欄Ａ～Fに入る語句の組み合わせとして、最も適切なものを下記の解答群から選べ。
　倒産は企業が経済的に破たんすることである。ただし、法律的には「倒産」という概念は厳密に規定されていない。
　倒産処理の種類には様々なものがある。倒産した企業の財産を清算する（　Ａ　）の手続きと、倒産した企業の組織などを変更して事業の継続を図る（　Ｂ　）の手続きがある。（　Ａ　）の倒産処理には（　Ｃ　）と（　Ｄ　）があり、（　Ｂ　）の倒産処理には（　Ｅ　）と（　Ｆ　）がある。`,
    choices: [
      { label: "ア", text: "Ａ：清算型　Ｂ：再建型　Ｃ：民事再生　Ｄ：会社再生　Ｅ：特別清算　Ｆ：破産" },
      { label: "イ", text: "Ａ：再建型　Ｂ：清算型　Ｃ：民事再生　Ｄ：特別清算　Ｅ：会社更生　Ｆ：破産" },
      { label: "ウ", text: "Ａ：清算型　Ｂ：再建型　Ｃ：破産　Ｄ：特別清算　Ｅ：民事再生　Ｆ：会社更生" },
      { label: "エ", text: "Ａ：再建型　Ｂ：清算型　Ｃ：破産　Ｄ：会社更生　Ｅ：民事再生　Ｆ：特別清算" },
    ],
    answer: "ウ",
    fig: "insolvency",
    note: `ここが重要
　本問では倒産の処理について問われています。
　倒産処理の種類には、法律に従って行う「法的整理」と、債務者と債権者が合意によって処理する「私的整理」があります。
　さらに法的整理には、倒産した企業の財産を清算する「清算型」の手続きと、倒産した企業の組織などを変更して事業の継続を図る「再建型」の手続きがあります。図示すると以下の通りです。
[[FIG]]
Ａ～Fに入る語句は次のようになります。
Ａ：清算型
　「倒産した企業の財産を清算する」という部分から清算型と分かります。
Ｂ：再建型
　「倒産した企業の組織などを変更して事業の継続を図る」という部分から再建型と分かります。
Ｃ・Ｄ：破産、特別清算(順不同）
　清算型の倒産処理には、破産と特別清算があります。
Ｅ・Ｆ：民事再生、会社更生(順不同）
　再建型の倒産処理には、民事再生と会社更生があります。
以上より、正解はウとなります。`,
  },
  {
    id: 18,
    source: "スマート問題集 6-7",
    kind: "4択問題",
    category: 1,
    title: "問題18 相続",
    question: "相続に関する法律について、最も不適切なものはどれか。",
    choices: [
      { label: "ア", text: "個人事業主が死亡した場合、事業上のものであるか否かを問わず、その者の積極財産、消極財産はすべて相続の対象となる。" },
      { label: "イ", text: "相続人が、相続によって得た財産の限度においてのみ、被相続人の債務を弁済すべきことを留保して相続の承認をすることができ、これを限定承認という。" },
      { label: "ウ", text: "負債を相続したくないときには、家庭裁判所で相続放棄の手続きを行うと、初めから相続人にならなかったとみなされる。" },
      { label: "エ", text: "遺産分割協議によって、相続人は遺産を分割することができる。この場合の遺産とは積極財産ばかりではなく、消極財産も含まれる。" },
    ],
    answer: "エ",
    fig: null,
    note: `ここが重要
　本問では相続について問われています。
　診断士の経営法務においては、企業のビジネスに関連した知識が問われるだけではなく、相続などの民法に関する問題も出題されています。
　要領よく、重要ポイントだけをおさえておきましょう。
ア　○：　一般的な中小企業者・事業者では、金融機関の借入に際して代表者が連帯保証人になっていることや、家屋を担保にしていることがあります。そのため、「親族承継」の場合は、資産だけでなく債務について、相続の問題が絡んできます。個人事業主が死亡した場合、事業上のものであるか否かを問わず、その者の積極財産（プラスの財産）、消極財産（マイナスの財産）はすべて相続の対象となります（民法896条本文）。
イ　○：　相続人が、相続によって得た財産の限度においてのみ、被相続人の債務を弁済すべきことを留保して相続の承認をすることができます。これを限定承認といいます（民法922条）。相続財産が明らかに債務超過の場合には、次に説明する相続放棄をすればよいですが、資産と負債の額が明らかでない場合には、限定承認をすることによってプラスの財産を取得できる可能性があるというメリットがあります。
ウ　○：　負債を相続したくないときには、家庭裁判所で相続放棄の手続きを行います（民法938条）。相続放棄をすると、初めから相続人にならなかったとみなされます（民法939条）。この手続きも、上記限定承認と同様に相続開始があったことを知った日から3か月以内に行う必要があります。
エ　×：　遺産分割協議によって、相続人は遺産を分割することができます（民法907条）。この場合の遺産とは積極財産（プラスの財産）のみを指し、消極財産（マイナスの財産、つまり負債）は遺産分割の対象とはなりません。負債は、各相続人の相続分に応じて分割して相続されることになります。したがって、本肢は誤っています。
　令和5年施行の改正民法により、被相続人死亡後に長時間遺産分割されない場合を解消するしくみとして、10年経過後の遺産分割は原則として法定相続分によって行うこととされました。`,
  },
  {
    id: 19,
    source: "スマート問題集 6-7",
    kind: "4択問題",
    category: 1,
    title: "問題19 相続2",
    question: `被相続人Ｘが死亡し、相続が生じた。ＡはＸの配偶者であり、ＢはＸ及びＡの子である。また、ＣはＢの子である。Ｘには兄弟姉妹Ｄがおり、ＥはＤの子である。さらに、Ｘには内縁関係のＧがいる。その他の親族はＸの死亡より前にすべて死亡している。
　この場合、Ｘの相続財産について、それぞれの相続人が相続する割合として、最も適切なものはどれか。
　なお、特に断りがない限り、各相続人は相続について承認したものとする。
　また、遺言はなく、遺産分割協議も整っておらず、相続人はいずれも廃除されていないものとし、寄与分及び特別受益についても考慮しないものとする。`,
    choices: [
      { label: "ア", text: "Ｘの死亡以前にＢが死亡していた場合、Ａが4分の3、Ｄが4分の1を相続する。" },
      { label: "イ", text: "Ｂが相続放棄をした場合、Ａが2分の1、Ｃが2分の1を相続する。" },
      { label: "ウ", text: "Ｘの死亡以前にＤが死亡しており、Ｂが相続放棄をした場合、Ａが4分の3、Ｅが4分の1を相続する。" },
      { label: "エ", text: "Ｘの死亡以前にＡ、Ｂ、Ｃ、Ｄ、Ｅのすべてが死亡していた場合に、ＧがＸの相続財産のすべてを相続する。" },
    ],
    answer: "ウ",
    fig: null,
    note: `ここが重要
　本問では法定相続分について問われています。
法定相続分は遺留分や経営承継円滑化法の特例による遺留分の例外を考えるうえでも基礎となるため、正確に理解しておく必要があります。
　まずは基本の法定相続分を確認しましょう。原則として以下の①、②が相続人となります。
　①配偶者
　②1)子、2)直系尊属、3)兄弟姉妹
　配偶者は必ず相続人となります。一方、②の相続人は1)～3)の順番に相続人となり、前の順位の者がいる場合には後の順位の者は相続人とはなりません。
　法定相続分については、①、②のいずれの相続人もいる場合には、次の割合で分けます。
　(1) 配偶者：2分の1、子：2分の1
　(2) 配偶者：3分の2、直系尊属：3分の1
　(3) 配偶者：4分の3、兄弟姉妹：4分の1
　ここまでは基本ですので確実に押さえておきましょう。そのうえで、さらに細かい規定については問題演習を通じて把握するようにしてください。
まずは状況を簡単に整理してみましょう。相続人の候補として、配偶者Ａ、子Ｂ、兄弟姉妹Ｄがいます。したがって、何事もなければ、Ａが2分の1、Ｂが2分の1を相続します。これが基本です。
ア　×：　Ｘの死亡以前にＢが死亡しています。このとき、②の順位に従えば兄弟姉妹Ｄが相続すると考えられますが、Ｂに子がいる場合にはその子が相続人となります（代襲相続）。したがって、本肢では、Ａが2分の1、Ｃが2分の1を相続します。
イ　×：　Ｂが相続放棄をしています。選択肢アの死亡とは異なり、相続放棄の場合には代襲相続はありません。したがって、②の順位に従い、（Ｂが相続放棄をしているため）兄弟姉妹Ｄが相続人となります。相続分はＡが4分の3、Ｄが4分の1です。
ウ　○：　Ｂが相続放棄をしているため、選択肢イと同様にＡ、Ｄが相続人となります。ところが本肢ではＸの死亡以前にＤが死亡しています。この場合も代襲相続があり、Ｅが相続人となります。したがって、Ａが4分の3、Ｅが4分の1を相続します。
エ　×：　ＧはＸの配偶者ではありませんので、相続人となる資格はありません。なお、本肢では相続人が不存在となるため、Ｇが特別縁故者として財産分与の対象となる可能性はあります。`,
  },
  {
    id: 20,
    source: "スマート問題集 6-7",
    kind: "4択問題",
    category: 1,
    title: "問題20 遺留分",
    question: "遺留分に関する法律について、最も不適切なものはどれか。",
    choices: [
      { label: "ア", text: "遺留分は、被相続人の財産のうち、一定の割合の承継を法定相続人に保証する制度であるが、兄弟姉妹には認められていない。" },
      { label: "イ", text: "直系尊属のみが相続人の場合、被相続人の財産の3分の１が遺留分である。" },
      { label: "ウ", text: "遺留分侵害額請求は相続開始および遺留分を侵害する贈与等のあったことを知った時から1年以内に行わなければならない。" },
      { label: "エ", text: "遺留分の権利者は、遺留分を侵害している受遺者や受贈者に対して、遺留分侵害額に相当する金銭の支払を請求することはできない。" },
    ],
    answer: "エ",
    fig: null,
    note: `ここが重要
　本問では遺留分について問われています。
遺産のうち一定の相続人のために最低限の相続財産が保障されているが、これを遺留分（民法1042条）といいます。
遺留分の権利を持つのは、配偶者、子（代襲相続人を含む）、直系尊属（被相続人の父・母など）です。直系尊属のみが相続人である場合には、相続財産全体の3分の1、それ以外の場合には2分の1が遺留分権利者全体の遺留分となります。なお、被相続人の兄弟姉妹には、遺留分はありません。
本問は、遺留分や遺留分侵害請求権について取り上げました。要領よく、重要ポイントをおさえておきましょう。
ア　○：　上記の通り、遺産のうち一定の相続人のためにどうしても残さなければいけない部分を遺留分（民法1042条）といいますが、被相続人の兄弟姉妹には、遺留分はありません。
イ　○：　相続人が父母や祖父母などの直系尊属のみの場合、遺留分は被相続人の財産の３分の１です。それ以外の場合は、被相続人の財産の２分の１が遺留分となります。
ウ　○：　遺留分を侵害された相続人は、「遺留分侵害額請求権」を行使することで初めて、遺言によって侵害された遺留分を取り戻すことができます。遺留分侵害額請求は、相続開始および遺留分を侵害する贈与等のあったことを知った時から1年以内に行わなければなりません。
エ　×：　遺留分侵害額請求は、遺留分権利者およびその承継人が行うことができ遺留分を侵害している受遺者や受贈者に対して、侵害額に相当する金銭の支払を請求することができます。よって、選択肢エは不適切です。
以上より、正解は選択肢エとなります。`,
  },
  {
    id: 21,
    source: "スマート問題集 6-7",
    kind: "4択問題",
    category: 1,
    title: "問題21 遺留分特例",
    question: `先代経営者甲が死亡し、配偶者乙は1年前に死亡していたため、その子であるＡ、Ｂ、Ｃが甲の財産を相続することになった。甲の有する財産は、不動産3,000万円のみである。また、Ａは後継者として甲の死亡の半年前に当時の価額で3,000万円の自社株式の贈与を受けており、当該株式は相続開始の時に1億2,000万円に価値が上昇していた。
遺留分に関する民法の特例の説明として、最も適切なものはどれか。`,
    choices: [
      { label: "ア", text: "Ａが生前贈与を受けた自社株式3,000万円について除外合意の対象となっている場合、Ｂの遺留分の額は750万円である。" },
      { label: "イ", text: "Ａが生前贈与を受けた自社株式3,000万円についてこの額で固定合意の対象となっている場合、Ｂの遺留分の額は1,500万円である。" },
      { label: "ウ", text: "除外合意や固定合意などの特段の合意がない場合、Ｂの遺留分の額は2,500万円である。" },
      { label: "エ", text: "Ａが生前贈与を受けた自社株式3,000万円のうち、1,500万円が除外合意の対象となり、残りの1,500万円がこの額で固定合意の対象となっている場合、Ｂの遺留分の額は1,125万円である。" },
    ],
    answer: "ウ",
    fig: null,
    note: `ここが重要
　本問では遺留分に関する民法の特例について問われています。
中小企業の円滑な経営承継を支援すべく中小企業経営承継円滑化法が制定されています。このなかで遺留分に関する民法の特例として、除外合意や固定合意の制度が設けられています。
　●除外合意
　贈与株式等を遺留分算定基礎財産から除外することができます。
　●固定合意
　贈与株式等の評価額をあらかじめ固定することができます。
　除外合意と固定合意は組み合わせて利用することも可能です。
　中小企業経営承継円滑化法に関しては、確実に押さえておきたいテーマです。
ア　×：　贈与株式3,000万円について除外合意の対象となっているため、遺留分算定基礎財産は不動産3,000万円のみとなります。したがって、遺留分の総額は遺留分算定基礎財産の2分の1である1,500万円であり、これをＡ、Ｂ、Ｃの3人で等分するとＢの遺留分の額は500万円となります。
イ　×：　贈与株式3,000万円についてこの額で固定合意の対象となっているため、遺留分算定基礎財産は不動産3,000万円と贈与株式3,000万円を合わせた6,000万円となります。したがって、遺留分の総額は遺留分算定基礎財産の2分の1である3,000万円であり、これをＡ、Ｂ、Ｃの3人で等分するとＢの遺留分の額は1,000万円となります。
ウ　○：　除外合意や固定合意などの特段の合意がないため、遺留分算定基礎財産は不動産3,000万円と贈与株式（相続開始時の価額）1億2,000万円となります。したがって、遺留分の総額は遺留分算定基礎財産の2分の1である7,500万円であり、これをＡ、Ｂ、Ｃの3人で等分するとＢの遺留分の額は2,500万円となります。
エ　×：　贈与株式3,000万円のうち、1,500万円について除外合意の対象となり、残りの1,500万円についてこの額で固定合意の対象となっているため、遺留分算定基礎財産は不動産3,000万円と贈与株式1,500万円となります。したがって、遺留分の総額は遺留分算定基礎財産の2分の1である2,250万円であり、これをＡ、Ｂ、Ｃの3人で等分するとＢの遺留分の額は750万円となります。`,
  },
];

// 大カテゴリ（レーダーチャート用）
// CAT1: 民法・契約・相続（債権法／相続法） ／ CAT2: 経済法規・国際取引（ビジネス関連法規）
const CAT1_IDS = [1, 2, 3, 4, 5, 6, 7, 8, 9, 18, 19, 20, 21];
const CAT2_IDS = [10, 11, 12, 13, 14, 15, 16, 17];

/* ===================== LocalStorage / 進捗ユーティリティ ===================== */
const lsKey = (userId) => `${APP_ID}::${userId}`;

function loadLocal(userId) {
  try {
    const raw = localStorage.getItem(lsKey(userId));
    if (!raw) return {};
    return JSON.parse(raw) || {};
  } catch (e) {
    console.warn("[local] 読み込み失敗、空で復帰", e);
    return {};
  }
}

function saveLocal(userId, data) {
  try {
    localStorage.setItem(lsKey(userId), JSON.stringify(data || {}));
  } catch (e) {
    console.warn("[local] 保存失敗", e);
  }
}

function parseProgress(data) {
  const d = data || {};
  return {
    progressIndex: Number(d.progressIndex || 0),
    progressMode: d.progressMode || "all",
    history: d.history || {},
    reviews: d.reviews || {},
  };
}

/* ===================== インラインSVG／テーブル図表（画像URL不使用・100%内製） ===================== */

// 問題5：連帯保証の関係図（B債権者 → A債務者／C保証人 を輪で結ぶ）
function GuaranteeFig() {
  const Person = ({ x, y, color }) => (
    <g transform={`translate(${x}, ${y})`}>
      <circle cx="0" cy="-14" r="9" fill={color} />
      <path d="M-15 16 C-15 2 -9 -2 0 -2 C9 -2 15 2 15 16 Z" fill={color} />
    </g>
  );
  return (
    <div className="overflow-x-auto rounded-xl border border-slate-700 bg-slate-950/60 p-3">
      <svg viewBox="0 0 520 300" className="mx-auto h-auto w-full max-w-[520px]" xmlns="http://www.w3.org/2000/svg">
        <defs>
          <marker id="arrowRed" markerWidth="10" markerHeight="10" refX="8" refY="3" orient="auto" markerUnits="strokeWidth">
            <path d="M0,0 L8,3 L0,6 Z" fill="#f43f5e" />
          </marker>
        </defs>
        {/* 矢印（B→A, B→C） */}
        <line x1="120" y1="160" x2="368" y2="82" stroke="#f43f5e" strokeWidth="9" strokeLinecap="round" markerEnd="url(#arrowRed)" opacity="0.85" />
        <line x1="120" y1="160" x2="368" y2="236" stroke="#f43f5e" strokeWidth="9" strokeLinecap="round" markerEnd="url(#arrowRed)" opacity="0.85" />
        {/* 連帯保証を示す輪（赤い楕円） */}
        <ellipse cx="250" cy="159" rx="20" ry="52" fill="none" stroke="#ef4444" strokeWidth="3" />
        <text x="250" y="44" textAnchor="middle" fontSize="20" fontWeight="bold" fill="#f87171">連帯保証</text>
        {/* 人物 */}
        <Person x="96" y="150" color="#818cf8" />
        <Person x="392" y="72" color="#38bdf8" />
        <Person x="392" y="226" color="#38bdf8" />
        {/* ラベル */}
        <text x="96" y="200" textAnchor="middle" fontSize="15" fontWeight="bold" fill="#c7d2fe">B：債権者</text>
        <text x="392" y="118" textAnchor="middle" fontSize="15" fontWeight="bold" fill="#bae6fd">A：債務者</text>
        <text x="392" y="272" textAnchor="middle" fontSize="15" fontWeight="bold" fill="#bae6fd">C：保証人</text>
      </svg>
    </div>
  );
}

// 問題9：担保物権の効力表（優先弁済効力／留置的効力）
function SecurityTableFig() {
  const rows = [
    { name: "留置権", yusen: "×", ryuchi: "○" },
    { name: "先取特権", yusen: "○", ryuchi: "×" },
    { name: "質権", yusen: "○", ryuchi: "○" },
    { name: "抵当権", yusen: "○", ryuchi: "×" },
  ];
  const Mark = ({ v }) =>
    v === "○" ? (
      <span className="font-bold text-emerald-400">○</span>
    ) : (
      <span className="font-bold text-rose-400">×</span>
    );
  return (
    <div className="overflow-x-auto rounded-xl border border-slate-700 bg-slate-950/60 p-3">
      <table className="w-full min-w-[360px] border-collapse text-center text-sm">
        <thead>
          <tr>
            <th className="border border-slate-700 bg-slate-800/60 px-3 py-2"></th>
            <th className="border border-slate-700 bg-slate-800/60 px-3 py-2 font-semibold text-slate-200">優先弁済効力</th>
            <th className="border border-slate-700 bg-slate-800/60 px-3 py-2 font-semibold text-slate-200">留置的効力</th>
          </tr>
        </thead>
        <tbody>
          {rows.map((r) => (
            <tr key={r.name}>
              <td className="border border-slate-700 bg-slate-900/60 px-3 py-2 font-semibold text-slate-200">{r.name}</td>
              <td className="border border-slate-700 px-3 py-2"><Mark v={r.yusen} /></td>
              <td className="border border-slate-700 px-3 py-2"><Mark v={r.ryuchi} /></td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  );
}

// 問題14：FOBとCIFの概略（海上輸送・費用関係）
function FobCifFig() {
  return (
    <div className="overflow-x-auto rounded-xl border border-slate-700 bg-slate-950/60 p-3">
      <svg viewBox="0 0 520 240" className="mx-auto h-auto w-full max-w-[520px]" xmlns="http://www.w3.org/2000/svg">
        <text x="260" y="26" textAnchor="middle" fontSize="15" fontWeight="bold" fill="#e2e8f0">FOBとCIFの概略</text>
        {/* 見出しボックス */}
        <rect x="150" y="42" width="220" height="34" rx="6" fill="#1e293b" stroke="#475569" />
        <text x="260" y="64" textAnchor="middle" fontSize="14" fontWeight="bold" fill="#cbd5e1">海上輸送・費用関係</text>
        {/* 船積・荷揚 */}
        <ellipse cx="90" cy="110" rx="42" ry="20" fill="#0f172a" stroke="#64748b" strokeWidth="1.5" />
        <text x="90" y="115" textAnchor="middle" fontSize="14" fill="#e2e8f0">船積</text>
        <ellipse cx="430" cy="110" rx="42" ry="20" fill="#0f172a" stroke="#64748b" strokeWidth="1.5" />
        <text x="430" y="115" textAnchor="middle" fontSize="14" fill="#e2e8f0">荷揚</text>
        {/* CIF 売主負担バー（船積→荷揚 全区間） */}
        <rect x="90" y="150" width="340" height="26" rx="4" fill="#0ea5e9" opacity="0.85" />
        <text x="260" y="168" textAnchor="middle" fontSize="13" fontWeight="bold" fill="#f0f9ff">CIF　売主負担</text>
        {/* FOB 買主負担バー（船積以降を買主が負担＝右寄せ） */}
        <rect x="150" y="186" width="280" height="26" rx="4" fill="#f59e0b" opacity="0.9" />
        <text x="290" y="204" textAnchor="middle" fontSize="13" fontWeight="bold" fill="#3b2600">FOB　買主負担</text>
        {/* 補助縦線 */}
        <line x1="90" y1="130" x2="90" y2="216" stroke="#475569" strokeWidth="1" strokeDasharray="4 3" />
        <line x1="430" y1="130" x2="430" y2="216" stroke="#475569" strokeWidth="1" strokeDasharray="4 3" />
      </svg>
    </div>
  );
}

// 問題17：倒産処理の種類（樹状図）
function InsolvencyFig() {
  const Node = ({ x, y, w = 96, label, fill = "#1e293b", stroke = "#475569", text = "#e2e8f0", bold }) => (
    <g>
      <rect x={x - w / 2} y={y} width={w} height="30" rx="6" fill={fill} stroke={stroke} strokeWidth="1.4" />
      <text x={x} y={y + 20} textAnchor="middle" fontSize="13" fontWeight={bold ? "bold" : "600"} fill={text}>
        {label}
      </text>
    </g>
  );
  const L = (x1, y1, x2, y2) => (
    <line x1={x1} y1={y1} x2={x2} y2={y2} stroke="#64748b" strokeWidth="1.4" />
  );
  return (
    <div className="overflow-x-auto rounded-xl border border-slate-700 bg-slate-950/60 p-3">
      <svg viewBox="0 0 720 260" className="mx-auto h-auto w-full max-w-[720px]" xmlns="http://www.w3.org/2000/svg">
        <text x="360" y="20" textAnchor="middle" fontSize="15" fontWeight="bold" fill="#e2e8f0">倒産処理の種類</text>
        {/* Lv1 倒産 */}
        <Node x={400} y={32} label="倒産" fill="#312e81" stroke="#6366f1" text="#e0e7ff" bold />
        {/* Lv1→Lv2 */}
        {L(400, 62, 400, 78)}
        {L(227, 78, 585, 78)}
        {L(227, 78, 227, 92)}
        {L(585, 78, 585, 92)}
        {/* Lv2 法的整理 / 私的整理 */}
        <Node x={227} y={92} w={110} label="法的整理" fill="#1e3a5f" stroke="#38bdf8" text="#bae6fd" bold />
        <Node x={585} y={92} w={110} label="私的整理" fill="#1e3a5f" stroke="#38bdf8" text="#bae6fd" bold />
        {/* 法的整理 → 再建型/清算型 */}
        {L(227, 122, 227, 136)}
        {L(122, 136, 332, 136)}
        {L(122, 136, 122, 150)}
        {L(332, 136, 332, 150)}
        <Node x={122} y={150} w={92} label="再建型" />
        <Node x={332} y={150} w={92} label="清算型" />
        {/* 私的整理 → 再建型/清算型 */}
        {L(585, 122, 585, 136)}
        {L(530, 136, 640, 136)}
        {L(530, 136, 530, 150)}
        {L(640, 136, 640, 150)}
        <Node x={530} y={150} w={92} label="再建型" />
        <Node x={640} y={150} w={92} label="清算型" />
        {/* 再建型(法的) → 会社更生法/民事再生法 */}
        {L(122, 180, 122, 196)}
        {L(70, 196, 175, 196)}
        {L(70, 196, 70, 210)}
        {L(175, 196, 175, 210)}
        <Node x={70} y={210} w={96} label="会社更生法" fill="#0f172a" stroke="#475569" />
        <Node x={175} y={210} w={96} label="民事再生法" fill="#0f172a" stroke="#475569" />
        {/* 清算型(法的) → 破産法/特別清算 */}
        {L(332, 180, 332, 196)}
        {L(280, 196, 385, 196)}
        {L(280, 196, 280, 210)}
        {L(385, 196, 385, 210)}
        <Node x={280} y={210} w={84} label="破産法" fill="#0f172a" stroke="#475569" />
        <Node x={385} y={210} w={88} label="特別清算" fill="#0f172a" stroke="#475569" />
      </svg>
    </div>
  );
}

function Figure({ id }) {
  switch (id) {
    case "guarantee":
      return <GuaranteeFig />;
    case "securityTable":
      return <SecurityTableFig />;
    case "fobcif":
      return <FobCifFig />;
    case "insolvency":
      return <InsolvencyFig />;
    default:
      return null;
  }
}

/* ===================== テキスト整形描画（改行・構造行を保持し、図表マーカーを差し込む） ===================== */
const CHOICE_MARK = /^([ア-エ])\s*([○〇×])\s*[:：]?\s*(.*)$/;

function RichText({ text, fig }) {
  const lines = (text || "").split("\n");
  return (
    <div className="space-y-1.5">
      {lines.map((ln, i) => {
        const t = ln.trim();
        if (t === "[[FIG]]") {
          return (
            <div key={i} className="my-3">
              <Figure id={fig} />
            </div>
          );
        }
        if (t === "") return <div key={i} className="h-1" />;
        // 見出し行「ここが重要」
        if (t === "ここが重要") {
          return (
            <p key={i} className="text-sm font-bold text-sky-300">
              {ln}
            </p>
          );
        }
        // ●で始まる小見出し
        if (t.startsWith("●")) {
          return (
            <p key={i} className="pt-0.5 text-sm font-bold text-indigo-300">
              {ln}
            </p>
          );
        }
        // 選択肢別解説（ア ○： / イ ×： …）
        const m = ln.match(CHOICE_MARK);
        if (m) {
          const ok = m[2] === "○" || m[2] === "〇";
          return (
            <p key={i} className="text-sm leading-relaxed text-slate-300">
              <span className={`font-bold ${ok ? "text-emerald-400" : "text-rose-400"}`}>
                {m[1]}　{m[2]}：
              </span>
              {m[3]}
            </p>
          );
        }
        return (
          <p key={i} className="text-sm leading-relaxed text-slate-300">
            {ln}
          </p>
        );
      })}
    </div>
  );
}

/* ===================== ロゴ（インラインSVG） ===================== */
function Logo({ size = 40 }) {
  return (
    <svg width={size} height={size} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg">
      <defs>
        <linearGradient id="lg" x1="0" y1="0" x2="48" y2="48" gradientUnits="userSpaceOnUse">
          <stop stopColor="#6366f1" />
          <stop offset="1" stopColor="#0ea5e9" />
        </linearGradient>
      </defs>
      <rect x="3" y="3" width="42" height="42" rx="12" fill="url(#lg)" />
      <path d="M14 32V17h6.5a4.5 4.5 0 0 1 0 9H14" stroke="#fff" strokeWidth="2.6" strokeLinecap="round" strokeLinejoin="round" />
      <path d="M20 26l8 6" stroke="#e0e7ff" strokeWidth="2.6" strokeLinecap="round" strokeLinejoin="round" />
      <circle cx="33" cy="17" r="2.4" fill="#c7d2fe" />
    </svg>
  );
}

/* ===================== メインコンポーネント ===================== */
export default function App() {
  const [userId, setUserId] = useState(null);
  const [inputPass, setInputPass] = useState("");
  const [isAuthenticated, setIsAuthenticated] = useState(false);
  const [loginLoading, setLoginLoading] = useState(false);
  const [loginError, setLoginError] = useState("");

  // 画面状態（"dashboard" | "quiz" | "result"）。ログイン画面は isAuthenticated で出し分け。
  const [screen, setScreen] = useState("dashboard");
  const screenRef = useRef(screen);
  useEffect(() => {
    screenRef.current = screen;
  }, [screen]);

  const [progress, setProgress] = useState({
    progressIndex: 0,
    progressMode: "all",
    history: {},
    reviews: {},
  });

  const [mode, setMode] = useState("all");
  const [quizList, setQuizList] = useState([]);
  const [currentIndex, setCurrentIndex] = useState(0);
  const [selected, setSelected] = useState(null);
  const [isAnswered, setIsAnswered] = useState(false);

  const [showResumeModal, setShowResumeModal] = useState(false);
  const [pendingProgress, setPendingProgress] = useState(null);

  // ユーザーID変更時に初回ロードフラグをリセット
  const isFirstLoad = useRef(true);
  useEffect(() => {
    isFirstLoad.current = true;
  }, [userId]);

  /* ---------- 出題リスト構築 ---------- */
  const buildList = (m, hist, rev) => {
    if (m === "wrong")
      return QUESTIONS.filter((q) => hist?.[q.id] && hist[q.id].correct === false);
    if (m === "review") return QUESTIONS.filter((q) => rev?.[q.id]);
    return QUESTIONS;
  };

  /* ---------- 永続化（Firestore + LocalStorage フォールバック） ---------- */
  const persist = async (patch) => {
    const next = {
      progressIndex:
        patch.progressIndex !== undefined ? patch.progressIndex : progress.progressIndex,
      progressMode: patch.progressMode !== undefined ? patch.progressMode : progress.progressMode,
      history: patch.history !== undefined ? patch.history : progress.history,
      reviews: patch.reviews !== undefined ? patch.reviews : progress.reviews,
    };
    setProgress(next);
    if (!userId) return;
    saveLocal(userId, next);
    if (firebaseReady && db) {
      try {
        await setDoc(doc(db, APP_ID, userId), next, { merge: true });
        console.log("[persist] Firestore 保存完了", {
          progressIndex: next.progressIndex,
          progressMode: next.progressMode,
        });
      } catch (e) {
        console.warn("[persist] Firestore 保存失敗。LocalStorage のみ保持", e);
      }
    }
  };

  /* ---------- 合言葉ログイン ---------- */
  const handleLogin = async () => {
    const id = inputPass.trim();
    if (!id) {
      setLoginError("合言葉を入力してください。");
      return;
    }
    setLoginLoading(true);
    setLoginError("");
    console.log("[login] 合言葉入力により接続開始:", id);
    try {
      if (firebaseReady && auth) {
        await signInAnonymously(auth);
        console.log("[login] 匿名認証成功（通信可能状態）。履歴フェッチを待機");
      }
      // ※ 認証成功だけでは isAuthenticated にしない。
      //   userId を確定させ、購読 effect 内の初回フェッチ完了で初めてゲートを開く。
      setUserId(id);
    } catch (e) {
      console.warn("[login] 認証失敗。LocalStorage モードで復元", e);
      // 認証失敗時も LocalStorage から復元して継続
      setUserId(id);
    }
  };

  /* ---------- 認証後：Firestore購読（screenRefガード付き） ---------- */
  useEffect(() => {
    if (!userId) return;
    let unsub = () => {};

    const openGate = (parsed) => {
      setProgress(parsed);
      setIsAuthenticated(true);
      setLoginLoading(false);
      console.log("[login] 履歴復元完了 → ログインゲート解放", {
        progressIndex: parsed.progressIndex,
        progressMode: parsed.progressMode,
      });
      // 初回ロードかつ画面がダッシュボードのときのみ途中再開モーダルをトリガー
      if (screenRef.current === "dashboard" && parsed.progressIndex > 0) {
        setPendingProgress(parsed);
        setShowResumeModal(true);
        console.log("[resume] 途中再開モーダル表示（問題", parsed.progressIndex + 1, "から）");
      }
    };

    if (firebaseReady && db) {
      const ref = doc(db, APP_ID, userId);
      unsub = onSnapshot(
        ref,
        (snapshot) => {
          const data = snapshot.exists() ? snapshot.data() : {};
          const parsed = parseProgress(data);

          if (isFirstLoad.current) {
            isFirstLoad.current = false;
            // クラウドに履歴が無ければローカルから補完
            const local = parseProgress(loadLocal(userId));
            if (!snapshot.exists() && local.progressIndex >= 0) {
              openGate(local);
            } else {
              openGate(parsed);
            }
            return;
          }

          // 【ガード】クイズ解答中などの更新では再開モーダルを出さず進捗のみ同期
          setProgress(parsed);
          console.log("[sync] onSnapshot 受信（モーダル抑止・進捗のみ同期） screen=", screenRef.current);
        },
        (err) => {
          console.warn("[sync] onSnapshot エラー。LocalStorage へフォールバック", err);
          if (isFirstLoad.current) {
            isFirstLoad.current = false;
            openGate(parseProgress(loadLocal(userId)));
          }
        }
      );
    } else {
      // LocalStorage モード
      if (isFirstLoad.current) {
        isFirstLoad.current = false;
        openGate(parseProgress(loadLocal(userId)));
      }
    }

    return () => unsub();
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [userId]);

  /* ---------- 出題開始 ---------- */
  const startQuiz = (m, startIndex = 0) => {
    const list = buildList(m, progress.history, progress.reviews);
    if (list.length === 0) {
      alert(
        m === "wrong"
          ? "前回不正解の問題はありません。"
          : m === "review"
          ? "要復習に登録された問題はありません。"
          : "出題できる問題がありません。"
      );
      return;
    }
    const idx = Math.min(startIndex, list.length - 1);
    setMode(m);
    setQuizList(list);
    setCurrentIndex(idx);
    setSelected(null);
    setIsAnswered(false);
    setScreen("quiz");
    console.log("[mode] 出題モード切替:", m, " 開始インデックス:", idx, " 問題数:", list.length);
    persist({ progressMode: m, progressIndex: idx });
  };

  /* ---------- 途中再開 ---------- */
  const handleResume = () => {
    const p = pendingProgress || progress;
    setShowResumeModal(false);
    console.log("[resume] 続きから再開:", p.progressMode, "index", p.progressIndex);
    startQuiz(p.progressMode || "all", p.progressIndex || 0);
  };

  const handleRestart = () => {
    setShowResumeModal(false);
    console.log("[resume] 最初から開始 → progressIndex を 0 にリセット");
    persist({ progressIndex: 0 });
  };

  /* ---------- 解答 ---------- */
  const handleAnswer = (label) => {
    if (isAnswered) return;
    const q = quizList[currentIndex];
    if (!q) return;
    const correct = label === q.answer;
    setSelected(label);
    setIsAnswered(true);

    const newHistory = {
      ...progress.history,
      [q.id]: {
        correct,
        selected: label,
        answeredAt: new Date().toISOString(),
      },
    };
    // 解答するたびに progressIndex を保存（途中再開用）
    const nextIndex = currentIndex + 1;
    console.log("[answer] 解答保存 Q", q.id, correct ? "正解" : "不正解", " progressIndex=", nextIndex);
    persist({
      history: newHistory,
      progressIndex: nextIndex >= quizList.length ? 0 : nextIndex,
      progressMode: mode,
    });
  };

  /* ---------- 次へ / 完走 ---------- */
  const handleNext = () => {
    if (currentIndex + 1 >= quizList.length) {
      console.log("[complete] 全問完走 → progressIndex を 0 にリセット");
      persist({ progressIndex: 0 });
      setScreen("result");
      return;
    }
    setCurrentIndex((i) => i + 1);
    setSelected(null);
    setIsAnswered(false);
  };

  /* ---------- 要復習トグル ---------- */
  const toggleReview = (qid) => {
    const cur = !!progress.reviews?.[qid];
    const newReviews = { ...progress.reviews };
    if (cur) delete newReviews[qid];
    else newReviews[qid] = true;
    console.log("[review] 要復習", cur ? "解除" : "登録", "Q", qid);
    persist({ reviews: newReviews });
  };

  /* ---------- ホームへ（途中インデックスを即保存） ---------- */
  const goHome = () => {
    if (screen === "quiz" && !isAnswered) {
      // 未解答で離脱する場合は現在地を保存
      console.log("[home] 中断 → progressIndex 保存:", currentIndex);
      persist({ progressIndex: currentIndex, progressMode: mode });
    }
    setScreen("dashboard");
    setSelected(null);
    setIsAnswered(false);
  };

  /* ---------- 集計 ---------- */
  const stats = useMemo(() => {
    const h = progress.history || {};
    const r = progress.reviews || {};
    let correct = 0,
      wrong = 0;
    QUESTIONS.forEach((q) => {
      if (h[q.id]) {
        if (h[q.id].correct) correct++;
        else wrong++;
      }
    });
    const answered = correct + wrong;
    const total = QUESTIONS.length;
    const untouched = total - answered;
    const reviewCount = QUESTIONS.filter((q) => r?.[q.id]).length;
    const catProgress = (ids) => {
      const done = ids.filter((id) => h[id]).length;
      return ids.length ? done / ids.length : 0;
    };
    return {
      correct,
      wrong,
      answered,
      untouched,
      total,
      reviewCount,
      progressRate: total ? answered / total : 0,
      correctRate: total ? correct / total : 0,
      accuracy: answered ? correct / answered : 0,
      cat1: catProgress(CAT1_IDS),
      cat2: catProgress(CAT2_IDS),
    };
  }, [progress]);

  const radarData = useMemo(
    () => [
      { metric: "総合進捗率", value: Math.round(stats.progressRate * 100) },
      { metric: "全問正解率", value: Math.round(stats.correctRate * 100) },
      { metric: "回答正確性", value: Math.round(stats.accuracy * 100) },
      { metric: "民法・相続", value: Math.round(stats.cat1 * 100) },
      { metric: "経済法規", value: Math.round(stats.cat2 * 100) },
    ],
    [stats]
  );

  /* ===================== 描画 ===================== */

  // --- ログイン画面（合言葉認証が完了するまで他を一切描画しない） ---
  if (!isAuthenticated) {
    return (
      <div className="min-h-screen bg-slate-950 text-slate-100 font-sans flex items-center justify-center px-4">
        <div className="w-full max-w-md">
          <div className="flex flex-col items-center mb-8">
            <Logo size={56} />
            <h1 className="mt-4 text-2xl font-bold tracking-tight">スマート問題集 6-7</h1>
            <p className="mt-1 text-sm text-slate-400">中小企業診断士 経営法務 ／ 契約とその他の法律知識</p>
          </div>

          <div className="rounded-2xl border border-slate-800 bg-slate-900/70 p-6 shadow-2xl shadow-indigo-950/40 backdrop-blur">
            <label className="mb-2 flex items-center gap-2 text-sm font-semibold text-slate-300">
              <User size={16} className="text-indigo-400" />
              合言葉（ユーザーID）
            </label>
            <input
              type="text"
              value={inputPass}
              onChange={(e) => setInputPass(e.target.value)}
              onKeyDown={(e) => {
                if (e.key === "Enter") handleLogin();
              }}
              placeholder="例: my-study-key"
              disabled={loginLoading}
              className="w-full rounded-xl border border-slate-700 bg-slate-950 px-4 py-3 text-slate-100 outline-none transition focus:border-indigo-500 focus:ring-2 focus:ring-indigo-500/30 disabled:opacity-60"
            />
            <p className="mt-2 text-xs leading-relaxed text-slate-500">
              同じ合言葉をPC・スマホで入力すれば、学習履歴と進捗が全端末で同期・復元されます。
            </p>
            {loginError && <p className="mt-2 text-xs text-rose-400">{loginError}</p>}

            <button
              onClick={handleLogin}
              disabled={loginLoading}
              className="mt-5 flex w-full items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-indigo-600 to-sky-500 px-4 py-3 font-bold text-white shadow-lg shadow-indigo-900/40 transition hover:scale-[1.01] hover:shadow-indigo-700/40 disabled:opacity-60"
            >
              {loginLoading ? (
                <>
                  <RefreshCw size={18} className="animate-spin" />
                  履歴を復元中...
                </>
              ) : (
                <>
                  学習をはじめる
                  <ArrowRight size={18} />
                </>
              )}
            </button>
          </div>

          <p className="mt-6 text-center text-xs text-slate-600">
            {firebaseReady ? "クラウド同期: 有効" : "ローカル保存モード（Firebase未設定）"}
          </p>
        </div>
      </div>
    );
  }

  const currentQ = quizList[currentIndex];

  /* ---------- 共通ヘッダー ---------- */
  const Header = () => (
    <header className="sticky top-0 z-20 border-b border-slate-800 bg-slate-950/80 backdrop-blur">
      <div className="mx-auto flex max-w-3xl items-center justify-between px-4 py-3">
        <div className="flex items-center gap-2.5">
          <Logo size={32} />
          <div>
            <div className="text-sm font-bold leading-tight">スマート問題集 6-7</div>
            <div className="text-[11px] text-slate-500">経営法務 ／ 契約とその他の法律知識</div>
          </div>
        </div>
        <button
          onClick={goHome}
          className="flex items-center gap-1.5 rounded-lg border border-slate-700 px-3 py-1.5 text-xs font-semibold text-slate-300 transition hover:border-indigo-500 hover:text-white"
        >
          <Home size={14} />
          ホーム
        </button>
      </div>
    </header>
  );

  /* ---------- ダッシュボード ---------- */
  if (screen === "dashboard") {
    return (
      <div className="min-h-screen bg-slate-950 text-slate-100 font-sans">
        <Header />

        {showResumeModal && (
          <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/80 px-4 backdrop-blur-sm">
            <div className="w-full max-w-sm rounded-2xl border border-slate-700 bg-slate-900 p-6 shadow-2xl">
              <div className="mb-3 flex items-center gap-2 text-indigo-400">
                <RefreshCw size={18} />
                <h3 className="text-base font-bold text-white">途中再開</h3>
              </div>
              <p className="text-sm leading-relaxed text-slate-300">
                前回は【問題 {(pendingProgress?.progressIndex || 0) + 1}】まで進んでいます。
                中断したモード（
                {pendingProgress?.progressMode === "wrong"
                  ? "前回不正解"
                  : pendingProgress?.progressMode === "review"
                  ? "要復習"
                  : "すべての問題"}
                モード）の続きから再開しますか？
              </p>
              <div className="mt-5 flex flex-col gap-2">
                <button
                  onClick={handleResume}
                  className="flex items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-indigo-600 to-sky-500 px-4 py-2.5 font-bold text-white transition hover:scale-[1.01]"
                >
                  続きから再開する
                  <ChevronRight size={16} />
                </button>
                <button
                  onClick={handleRestart}
                  className="rounded-xl border border-slate-700 px-4 py-2.5 text-sm font-semibold text-slate-300 transition hover:border-slate-500"
                >
                  最初から始める
                </button>
              </div>
            </div>
          </div>
        )}

        <main className="mx-auto max-w-3xl space-y-6 px-4 py-6">
          {/* 集計サマリー */}
          <section className="grid grid-cols-2 gap-3 sm:grid-cols-4">
            {[
              { label: "正解", value: stats.correct, cls: "text-emerald-400" },
              { label: "不正解", value: stats.wrong, cls: "text-rose-400" },
              { label: "未着手", value: stats.untouched, cls: "text-slate-400" },
              { label: "要復習", value: stats.reviewCount, cls: "text-amber-400" },
            ].map((s) => (
              <div
                key={s.label}
                className="rounded-xl border border-slate-800 bg-slate-900/60 p-3 text-center"
              >
                <div className={`text-2xl font-bold ${s.cls}`}>{s.value}</div>
                <div className="mt-0.5 text-[11px] text-slate-500">{s.label}</div>
              </div>
            ))}
          </section>

          {/* レーダーチャート */}
          <section className="rounded-2xl border border-slate-800 bg-slate-900/60 p-4">
            <div className="mb-1 flex items-center gap-2">
              <BarChart2 size={16} className="text-sky-400" />
              <h2 className="text-sm font-bold">学習分析レーダー</h2>
            </div>
            <div className="h-64 w-full">
              <ResponsiveContainer width="100%" height="100%">
                <RadarChart data={radarData} outerRadius="72%">
                  <PolarGrid stroke="#334155" />
                  <PolarAngleAxis
                    dataKey="metric"
                    tick={{ fill: "#cbd5e1", fontSize: 11 }}
                  />
                  <PolarRadiusAxis
                    angle={90}
                    domain={[0, 100]}
                    tick={{ fill: "#64748b", fontSize: 9 }}
                    stroke="#334155"
                  />
                  <Radar
                    name="達成度(%)"
                    dataKey="value"
                    stroke="#6366f1"
                    fill="#6366f1"
                    fillOpacity={0.45}
                  />
                  <Tooltip
                    contentStyle={{
                      background: "#0f172a",
                      border: "1px solid #334155",
                      borderRadius: 8,
                      color: "#e2e8f0",
                      fontSize: 12,
                    }}
                  />
                </RadarChart>
              </ResponsiveContainer>
            </div>
          </section>

          {/* モード選択 */}
          <section className="grid gap-3 sm:grid-cols-3">
            {[
              {
                m: "all",
                title: "すべての問題",
                desc: `全 ${QUESTIONS.length} 問`,
                icon: <BookOpen size={18} />,
              },
              {
                m: "wrong",
                title: "前回不正解のみ",
                desc: `${stats.wrong} 問`,
                icon: <X size={18} />,
              },
              {
                m: "review",
                title: "要復習のみ",
                desc: `${stats.reviewCount} 問`,
                icon: <HelpCircle size={18} />,
              },
            ].map((c) => (
              <button
                key={c.m}
                onClick={() => startQuiz(c.m, 0)}
                className="group flex flex-col items-start gap-2 rounded-2xl border border-slate-800 bg-slate-900/60 p-4 text-left transition hover:scale-[1.01] hover:border-indigo-500 hover:bg-slate-900"
              >
                <span className="rounded-lg bg-indigo-600/20 p-2 text-indigo-300 transition group-hover:bg-indigo-600/30">
                  {c.icon}
                </span>
                <div>
                  <div className="text-sm font-bold">{c.title}</div>
                  <div className="text-xs text-slate-500">{c.desc}</div>
                </div>
              </button>
            ))}
          </section>

          {/* 問題一覧（俯瞰グリッド） */}
          <section className="rounded-2xl border border-slate-800 bg-slate-900/60 p-4">
            <h2 className="mb-3 text-sm font-bold">問題一覧と解答状況</h2>
            <div className="space-y-2">
              {QUESTIONS.map((q) => {
                const h = progress.history?.[q.id];
                const rev = !!progress.reviews?.[q.id];
                const state = !h ? "untouched" : h.correct ? "correct" : "wrong";
                const badge =
                  state === "correct"
                    ? { t: "正解", c: "bg-emerald-500/15 text-emerald-400 border-emerald-500/30" }
                    : state === "wrong"
                    ? { t: "不正解", c: "bg-rose-500/15 text-rose-400 border-rose-500/30" }
                    : { t: "未着手", c: "bg-slate-700/30 text-slate-400 border-slate-600/40" };
                return (
                  <div
                    key={q.id}
                    className="flex items-center gap-3 rounded-xl border border-slate-800 bg-slate-950/40 px-3 py-2.5"
                  >
                    <span className="flex h-7 w-7 shrink-0 items-center justify-center rounded-lg bg-slate-800 text-xs font-bold text-slate-300">
                      {q.id}
                    </span>
                    <div className="min-w-0 flex-1">
                      <div className="truncate text-sm font-medium text-slate-200">{q.title}</div>
                      <div className="text-[11px] text-slate-500">
                        <span className="text-indigo-400/80">{q.source}</span>
                        {h?.answeredAt
                          ? ` ・ 最終解答: ${new Date(h.answeredAt).toLocaleString("ja-JP", {
                              month: "numeric",
                              day: "numeric",
                              hour: "2-digit",
                              minute: "2-digit",
                            })}`
                          : " ・ 未解答"}
                      </div>
                    </div>
                    {rev && (
                      <span className="rounded border border-amber-500/30 bg-amber-500/15 px-1.5 py-0.5 text-[10px] font-semibold text-amber-400">
                        要復習
                      </span>
                    )}
                    <span
                      className={`rounded border px-2 py-0.5 text-[11px] font-semibold ${badge.c}`}
                    >
                      {badge.t}
                    </span>
                  </div>
                );
              })}
            </div>
          </section>
        </main>
      </div>
    );
  }

  /* ---------- 出題・解説画面 ---------- */
  if (screen === "quiz" && currentQ) {
    const isReview = !!progress.reviews?.[currentQ.id];
    return (
      <div className="min-h-screen bg-slate-950 text-slate-100 font-sans">
        <Header />
        <main className="mx-auto max-w-3xl px-4 py-6">
          {/* 進捗バー */}
          <div className="mb-4">
            <div className="mb-1.5 flex items-center justify-between text-xs text-slate-400">
              <span>
                {currentIndex + 1} / {quizList.length} 問
              </span>
              <span>
                {mode === "all"
                  ? "すべての問題"
                  : mode === "wrong"
                  ? "前回不正解"
                  : "要復習"}
                モード
              </span>
            </div>
            <div className="h-1.5 w-full overflow-hidden rounded-full bg-slate-800">
              <div
                className="h-full rounded-full bg-gradient-to-r from-indigo-500 to-sky-400 transition-all"
                style={{ width: `${((currentIndex + 1) / quizList.length) * 100}%` }}
              />
            </div>
          </div>

          {/* 出典バッジ + タイトル */}
          <div className="mb-3 flex flex-wrap items-center gap-2">
            <span className="rounded-md border border-indigo-500/40 bg-indigo-500/15 px-2 py-1 text-[11px] font-semibold text-indigo-300">
              {currentQ.source}
            </span>
            <span className="rounded-md border border-violet-500/30 bg-violet-500/15 px-2 py-1 text-[11px] font-semibold text-violet-300">
              {currentQ.kind}
            </span>
            <span className="rounded-md bg-slate-800 px-2 py-1 text-[11px] font-semibold text-slate-300">
              問題 {currentQ.id}
            </span>
          </div>
          <div className="mb-3 text-sm font-semibold text-slate-200">{currentQ.title}</div>

          {/* 問題文 */}
          <div className="rounded-2xl border border-slate-800 bg-slate-900/60 p-5">
            <p className="whitespace-pre-wrap text-[15px] leading-relaxed text-slate-100">
              {currentQ.question}
            </p>
          </div>

          {/* 選択肢 */}
          <div className="mt-4 space-y-2.5">
            {currentQ.choices.map((c) => {
              const chosen = selected === c.label;
              const isCorrect = c.label === currentQ.answer;
              let cls =
                "border-slate-700 bg-slate-900/60 hover:border-indigo-500 hover:bg-slate-900";
              if (isAnswered) {
                if (isCorrect) cls = "border-emerald-500/60 bg-emerald-500/10";
                else if (chosen) cls = "border-rose-500/60 bg-rose-500/10";
                else cls = "border-slate-800 bg-slate-900/40 opacity-70";
              }
              return (
                <button
                  key={c.label}
                  onClick={() => handleAnswer(c.label)}
                  disabled={isAnswered}
                  className={`flex w-full items-start gap-3 rounded-xl border px-4 py-3 text-left transition ${cls} ${
                    !isAnswered ? "hover:scale-[1.005]" : ""
                  }`}
                >
                  <span
                    className={`mt-0.5 flex h-6 w-6 shrink-0 items-center justify-center rounded-md text-xs font-bold ${
                      isAnswered && isCorrect
                        ? "bg-emerald-500 text-white"
                        : isAnswered && chosen
                        ? "bg-rose-500 text-white"
                        : "bg-slate-800 text-slate-300"
                    }`}
                  >
                    {isAnswered && isCorrect ? (
                      <Check size={14} />
                    ) : isAnswered && chosen ? (
                      <X size={14} />
                    ) : (
                      c.label
                    )}
                  </span>
                  <span className="text-sm leading-relaxed text-slate-100">{c.text}</span>
                </button>
              );
            })}
          </div>

          {/* 解説 */}
          {isAnswered && (
            <div className="mt-5 space-y-4">
              {/* 正誤バナー */}
              <div
                className={`flex items-center gap-2 rounded-xl border px-4 py-3 ${
                  selected === currentQ.answer
                    ? "border-emerald-500/40 bg-emerald-500/10 text-emerald-300"
                    : "border-rose-500/40 bg-rose-500/10 text-rose-300"
                }`}
              >
                {selected === currentQ.answer ? <Check size={18} /> : <X size={18} />}
                <span className="font-bold">
                  {selected === currentQ.answer ? "正解！" : "不正解"}
                </span>
                <span className="ml-auto text-sm text-slate-300">
                  正解: <span className="font-bold text-white">{currentQ.answer}</span>
                </span>
              </div>

              {/* 要復習チェックボックス */}
              <label className="flex cursor-pointer items-center gap-2.5 rounded-xl border border-slate-800 bg-slate-900/60 px-4 py-3">
                <input
                  type="checkbox"
                  checked={isReview}
                  onChange={() => toggleReview(currentQ.id)}
                  className="h-4 w-4 accent-amber-500"
                />
                <span className="text-sm font-semibold text-slate-200">
                  この問題を要復習リストに登録する
                </span>
                {isReview && (
                  <span className="ml-auto rounded border border-amber-500/30 bg-amber-500/15 px-2 py-0.5 text-[11px] font-semibold text-amber-400">
                    登録中
                  </span>
                )}
              </label>

              {/* 解説・レジュメ（ここが重要 / 完全ノンカット・図表は解答後にのみ描画） */}
              <div className="rounded-2xl border border-sky-500/30 bg-sky-500/[0.06] p-5">
                <div className="mb-2 flex items-center gap-2 text-sky-300">
                  <BookOpen size={16} />
                  <h3 className="text-sm font-bold">解説・レジュメ（ここが重要）</h3>
                </div>
                <RichText text={currentQ.note} fig={currentQ.fig} />
              </div>

              {/* 次へ */}
              <button
                onClick={handleNext}
                className="flex w-full items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-indigo-600 to-sky-500 px-4 py-3.5 font-bold text-white shadow-lg shadow-indigo-900/40 transition hover:scale-[1.01]"
              >
                {currentIndex + 1 >= quizList.length ? "結果を見る" : "次の問題へ"}
                <ChevronRight size={18} />
              </button>
            </div>
          )}
        </main>
      </div>
    );
  }

  /* ---------- 結果画面 ---------- */
  if (screen === "result") {
    const answeredInList = quizList.filter((q) => progress.history?.[q.id]);
    const correctInList = answeredInList.filter((q) => progress.history?.[q.id]?.correct).length;
    return (
      <div className="min-h-screen bg-slate-950 text-slate-100 font-sans">
        <Header />
        <main className="mx-auto max-w-md px-4 py-12 text-center">
          <div className="mx-auto mb-6 flex h-20 w-20 items-center justify-center rounded-full bg-gradient-to-br from-indigo-600 to-sky-500 shadow-lg shadow-indigo-900/50">
            <Check size={40} className="text-white" />
          </div>
          <h2 className="text-2xl font-bold">お疲れさまでした！</h2>
          <p className="mt-2 text-slate-400">このセッションの全問題を完走しました。</p>

          <div className="mt-8 rounded-2xl border border-slate-800 bg-slate-900/60 p-6">
            <div className="text-5xl font-bold text-white">
              {correctInList}
              <span className="text-2xl text-slate-500"> / {quizList.length}</span>
            </div>
            <div className="mt-1 text-sm text-slate-400">正解数</div>
            <div className="mt-4 h-2 w-full overflow-hidden rounded-full bg-slate-800">
              <div
                className="h-full rounded-full bg-gradient-to-r from-indigo-500 to-sky-400"
                style={{
                  width: `${quizList.length ? (correctInList / quizList.length) * 100 : 0}%`,
                }}
              />
            </div>
          </div>

          <div className="mt-8 flex flex-col gap-2.5">
            <button
              onClick={() => startQuiz(mode, 0)}
              className="flex items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-indigo-600 to-sky-500 px-4 py-3 font-bold text-white transition hover:scale-[1.01]"
            >
              <RefreshCw size={16} />
              もう一度（同じモード）
            </button>
            <button
              onClick={goHome}
              className="flex items-center justify-center gap-2 rounded-xl border border-slate-700 px-4 py-3 font-semibold text-slate-300 transition hover:border-slate-500"
            >
              <Home size={16} />
              ホームに戻る
            </button>
          </div>
        </main>
      </div>
    );
  }

  /* ---------- フォールバック（想定外状態でも真っ白にしない） ---------- */
  return (
    <div className="min-h-screen bg-slate-950 text-slate-100 font-sans flex items-center justify-center">
      <button
        onClick={goHome}
        className="rounded-xl bg-indigo-600 px-6 py-3 font-bold text-white"
      >
        ホームに戻る
      </button>
    </div>
  );
}